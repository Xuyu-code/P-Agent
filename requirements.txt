# 河湟非遗知识问答与皮影创作 Agent · Python 依赖（Python 3.10/3.11）
#
# 安装（建议挂清华镜像）：
#   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# 版本已固定，避免上游升级破坏 API。
#
# 注意：本地嵌入（EMBEDDING_PROVIDER=local）需要额外安装 sentence-transformers
# （会拉入 torch，体积大，故不列入本文件）：
#   pip install sentence-transformers -i https://pypi.tuna.tsinghua.edu.cn/simple
#   $env:HF_ENDPOINT="https://hf-mirror.com"   # 首次下载 BAAI/bge-small-zh-v1.5 时

openai==3.3.1
httpx==0.28.1
pydantic==2.13.4
chromadb==1.5.9
langgraph==1.2.11
langchain-core==1.6.0
streamlit==1.62.0
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pytest==9.1.1
